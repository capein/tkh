This application listens on HTTPS port 8080.

Coupon code is only single use for run.

You must configure the environment variable `coupon_base_path` to point to the directory containing your coupon files.

Example:

export coupon_base_path=/path/to/coupons

